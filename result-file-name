#!/usr/bin/perl

use strict;
use warnings;

my $config_guess = `./config.guess 2>/dev/null`;
if ($? == 0) {
    chomp $config_guess;
    print "$config_guess.json\n";
}
else {
    print "results.json\n";
}
